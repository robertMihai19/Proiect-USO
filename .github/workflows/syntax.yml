name: syntax
on: push
jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "Adam Robert-Mihai"
      - run: gcc src/main.c -o main
      - run: |
          if [ "$(./main 1 2)" -eq "3" ]
          then
               echo "da bos"
               exit 0
          fi
